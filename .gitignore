/koment
/dist/
/bin/

# Assembled into the extension at package time from the canonical release.
/editors/vscode/bin/
/editors/vscode/icon.png
/editors/vscode/LICENSE

*.test
*.out
coverage.*

.DS_Store
node_modules/
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/mcp.json
!.vscode/settings.json
*.swp
.mise.local.toml
.mise.local.lock

# Local OpenCode CLI state. The repo's committed OpenCode config is
# `opencode.json` at the project root; everything under `.opencode/` is the
# CLI's own runtime scratch directory (see `docs/agents/opencode.md`).
/.opencode/
