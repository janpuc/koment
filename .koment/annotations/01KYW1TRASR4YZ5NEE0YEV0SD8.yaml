# yaml-language-server: $schema=https://raw.githubusercontent.com/janpuc/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KYW1TRASR4YZ5NEE0YEV0SD8
  created: "2026-07-31T00:00:00Z"
spec:
  target:
    file: internal/mcp/transport.go
  type: why
  title: mcp parses its own flags, so package cli never links the MCP SDK
  body: |-
    The mcp subcommand parses its own flags here rather than in package cli, so
    that cli never imports this package and therefore never links the MCP SDK.
    ADR 0007 accepted eight transitive modules only on the condition that they
    stay confined to internal/mcp, and this split is what keeps that true.
    Moving these flags into cli would spend the escape hatch without anyone
    noticing.
  anchor:
    scope: excerpt
    excerpt: func Serve(args []string, stderr io.Writer) error {
    before: |2-

      // Serve parses the mcp subcommand's own flags, so that package cli never links
      // the MCP SDK.
    after: "\tflags := flag.NewFlagSet(\"mcp\", flag.ContinueOnError)\n\tflags.SetOutput(stderr)\n\tflags.Usage = func() {"
  author:
    name: Legacy import
    kind: unknown
    source: migration
status:
  lastSeenLine: 41
