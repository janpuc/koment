# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ2WD0GQRHG6YRBBVPNJR5VM
  created: "2026-08-03T00:00:00Z"
spec:
  target:
    file: internal/mcp/mcp.go
  type: invariant
  title: An ambiguous path fails and names the candidates
  body: |-
    An ambiguous path fails and lists the candidates rather than choosing one.
    Answering from whichever repository the server happens to sit in is a
    confident wrong answer, which is the failure this whole project exists to
    prevent. Search behaves differently on purpose: searching everything and
    naming where each hit came from is useful, where refusing would not be. ADR
    0025.
  anchor:
    scope: excerpt
    excerpt: func repositoryForGet(repositories *repository.Set, named, file string) (repository.Repository, error) {
    before: "\treturn server\n}\n"
    after: "\tif named != \"\" {\n\t\tchosen, found := repositories.Resolve(named)\n\t\tif !found {"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: human
    source: git-config
  git:
    commit: 891a943c595ce0526139604cdd723ae6c63bf57b
    path: internal/mcp/mcp.go
    line: 48
status:
  lastSeenLine: 53
