# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ2Z60KXQ5Y1CDA9H7DQYX5W
  created: "2026-08-03T00:00:00Z"
spec:
  target:
    file: internal/ui/tree.go
  type: gotcha
  title: Root-level files come back separately, not inside a node
  body: |-
    Root-level files come back separately rather than inside a node. The root is
    not a row anyone should have to expand, and giving it one would file
    README.md under a directory that does not exist. An earlier version returned
    only root.Dirs and silently dropped every file at the top level.
  anchor:
    scope: excerpt
    excerpt: "\treturn root.Dirs, root.Files"
    before: "\n\tcollapse(root)\n\tsummarise(root, current)"
    after: |-
      }

      // directoryOf finds or creates the node for a slash-separated directory,
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 42
