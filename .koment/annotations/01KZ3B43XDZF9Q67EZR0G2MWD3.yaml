# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ3B43XDZF9Q67EZR0G2MWD3
  created: "2026-08-03T00:00:00Z"
spec:
  target:
    file: action.yml
  type: invariant
  title: Setup adds only a fixed RUNNER_TEMP subdir to PATH, never more
  body: |-
    The setup action adds only a fixed subdirectory of GitHub runner-owned
    RUNNER_TEMP to PATH. The downloaded executable is checksum-verified and the
    requested version is restricted to single-line SemVer before any output is
    written. The zizmor github-env suppression applies only to this fixed runner
    boundary; never derive target from an action input or release data.
  anchor:
    scope: excerpt
    excerpt: '        target="${RUNNER_TEMP}/koment-bin"'
    before: |2
                fi
              )
    after: |2-
              mkdir -p "$target"
              tar -xzf "${work}/${archive}" -C "$target" koment
              chmod +x "${target}/koment"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
  git:
    commit: 1d122963f024f48ed04833c6148369f92bbdebbc
    path: action.yml
    line: 89
status:
  lastSeenLine: 88
