# yaml-language-server: $schema=../../schema/server.schema.json
repositories:
  - id: koment
    name: koment
    provider: github
    remote: koment-dev/koment
    default_branch: main
    default: true
